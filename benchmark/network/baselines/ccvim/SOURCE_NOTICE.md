# Source notice

The files ending in `_upstream.py` are copied from the official CCViM
repository: https://github.com/zymissy/CCViM (retrieved 2026-09-11).
The upstream repository is Apache-2.0. `model.py` is a thin benchmark adapter
and requires a compiled selective-scan CUDA extension for forward inference.
