# SegNet

Place the SegNet implementation in this directory.

Required benchmark interface:

```python
model = SegNet(n_channels=3, n_classes=2)
logits = model(x)
```

Record the upstream repository URL, commit/tag, license, and any architecture
adaptations in this directory.
