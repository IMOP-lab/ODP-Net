# Source notice

The files ending in `_upstream.py` are copied from the official VM-UNet
repository: https://github.com/JCruan519/VM-UNet (retrieved 2026-09-11).
The upstream repository is Apache-2.0. `model.py` is a thin benchmark adapter;
the paper configuration uses reduced decoder depths and requires a compiled
selective-scan CUDA extension.
